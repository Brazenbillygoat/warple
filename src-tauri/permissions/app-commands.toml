[[permission]]
identifier = "companion-runtime"
description = "Allows the companion overlay to complete its one-shot startup handshake and read native cursor position."
commands.allow = ["get_mouse_position", "startup_ready", "abort_startup"]
